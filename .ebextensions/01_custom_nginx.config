files:
  "/etc/nginx/conf.d/my-nginx-config.conf":
    mode: "000755"
    owner: root
    group: root
    content: |
      location / {
          try_files $uri $uri/ /index.php?$query_string;
      }
